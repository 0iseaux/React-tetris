import React from 'react';
import {StyledBackButton} from './styles/StyledBackButton';

const BackButton = ({callback, text}) => <StyledBackButton>{text}</StyledBackButton>;

export default BackButton;
